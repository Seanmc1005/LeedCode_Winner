#include<stdlib.h>
#include<iostream>
#include<string>
#include<vector>
using namespace std;
int main() {
	string str;
	cin >> str;
	string temp = str;
	for (int i = str.length() - 1; i >= 0; i--) {
		temp += str[i];
	}
	cout << temp;
	return 0;
}
